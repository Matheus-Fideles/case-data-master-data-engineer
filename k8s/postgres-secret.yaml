apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: spark
type: Opaque
stringData:
  POSTGRES_USER: gold_engineer
  POSTGRES_PASSWORD: gold_engineer
  POSTGRES_JDBC_URL: jdbc:postgresql://host.docker.internal:5432/postgres
  # Salt para mascaramento PII (ADR-0004). Substituir por valor seguro em produção.
  PII_SALT: santander-case-dev-salt-change-in-prod
